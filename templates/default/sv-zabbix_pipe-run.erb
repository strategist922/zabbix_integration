#!/bin/bash
export PATH=$PATH:/opt/zabbix/bin
exec 2>&1
exec chpst -u zabbix zabbix_pipe --pipe=<%= node.zabbix_integration.pipe %> --fast --server=<%= @options[:server] %> --host=<%= @node[:node_name] %>
